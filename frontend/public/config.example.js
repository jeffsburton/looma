// Optional runtime configuration for the built SPA.
// Copy this file to frontend/public/config.js (before building) or upload a config.js next to index.html on your static host.
// Either way, it will be loaded before the app starts (see index.html) and can point the frontend at a remote API server.
//
// Option A: simple global
// window.__LOOMA_API_URL = "https://your-backend.example.com";
//
// Option B: structured config
// window.__APP_CONFIG__ = {
//   apiBaseUrl: "https://your-backend.example.com"
// };
