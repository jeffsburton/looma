// Optional runtime configuration for Looma frontend.
// This file intentionally left minimal to avoid 404s in development.
// You may set one of the following globals to configure API endpoint at runtime:
//   window.__LOOMA_API_URL = "http://127.0.0.1:8000";
// or
//   window.__APP_CONFIG__ = { apiBaseUrl: "http://127.0.0.1:8000" };

// If neither is set, the app will rely on Vite dev proxy (/api) as configured in vite.config.js.
